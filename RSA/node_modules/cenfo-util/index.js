module.exports = {
  no_imp: function(){
    // funcion dummy
    return ("no implementado");
  },
  gdc: function(a,n){
      if (n=0){
        return a;
      }
      else {
        return gdc(n, Math.mod(a, n));
        //return gdc(n, a mod n);
      }
  },
  obtener_e: function(p,q){
    // TODO implementar
    // e es la llave publica

    //return gdc(e,(p-1)*(q-1))
    return gdc(p,q)
  },
  obtener_d: function(p,q){
    // TODO Implementar
    // d es la llave privada

    //var d = e^-1 mod (p-1)(q-1)
      var d = Math.mod(Math.pow(e,-1), (p-1)(q-1))

    return (d);
  },
  obtener_n: function(p,q){
    // TODO implementar
    return (p*q);

  }
};
